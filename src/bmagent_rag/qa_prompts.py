"""???????????????? prompt ???

??????????? prompt????? query rewrite?????????????
???????????????????????????????????????
"""
from __future__ import annotations

from textwrap import dedent


def build_answer_system_prompt() -> str:
    """????????????????????????????????????
    """
    return dedent(

        '''
        你是“脑肿瘤 MRI 证据增强问答助手”。

        工作边界：
        - 只能基于应用传入的本地检索证据作答。
        - 如果证据不足，必须明确说证据不足，不能编造细节。
        - 重点回答四类问题：脑肿瘤 MRI 典型表现、影像鉴别、MRI 序列意义、论文/教材摘要。
        - 不要给出确定性诊断或治疗建议，只能给出影像学倾向和证据支持范围。

        输出要求：
        - 必须输出一个严格 JSON 对象，并满足给定 schema。
        - 语言固定为简体中文。
        - evidence 中的 excerpt 必须直接引用或紧密转述检索片段。
        - 如果证据不足，answer_type 设为 insufficient_evidence，并在 limitations 中说明原因。
        '''
    ).strip()


def build_system_prompt() -> str:
    """???????????????????????????
    """
    return build_answer_system_prompt()


def build_query_rewrite_prompt(question: str) -> str:
    """???????????????????????????????????
    """
    return dedent(
        f'''
        你在帮助一个本地文献检索系统改写查询。用户问题可能是中文，但文献标题和正文往往是英文。

        请只输出 JSON，格式固定为：
        {{"queries": ["query1", "query2", "query3"]}}

        要求：
        - 给出 1 到 3 条检索查询。
        - 至少 1 条尽量偏英文关键词风格。
        - 尽量保留肿瘤名称、MRI 序列、影像鉴别、review 等核心词。
        - 不要输出 JSON 之外的任何文字。

        用户问题：{question}
        '''
    ).strip()